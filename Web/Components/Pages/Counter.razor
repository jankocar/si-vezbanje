@page "/books"
@rendermode InteractiveServer
@using BusinessLayer;
@using DataLayer.Model

@inject   NavigationManager NV
@inject IBusinessBook bus

<PageTitle>Books</PageTitle>

<h1>Books</h1>
<button type="button" @onclick="Add" class="btn btn-primary" >Add Book</button>
<table class="table">
    <thead>
        <tr>
            <th scope="col">ID</th>
            <th scope="col">Title</th>
            <th scope="col">Description</th>
            <th scope="col">Number Of Page</th>
        </tr>
    </thead>
    <tbody>
        @if (Books != null)
        {
            @foreach (var item in Books)
            {
                <tr>

                    <td>@item.Id</td>
                    <td>@item.Title</td>
                    <td>@item.Description</td>
                    <td>@item.NumberOfPage</td>
                </tr>
            }
        }

    </tbody>
</table>


@code {
    private List<Book>? Books{ get; set; }

    protected override void OnInitialized()
    {
        Books = bus.GetBooksWith50();
    }

    private void Add()
    {
        NV.NavigateTo("/addBook");
    }
}

@* @page "/cars"
@rendermode InteractiveServer
@using BusinessLayer
@using DataLayer.Model

@inject IBusinessCar bus

<PageTitle>Cars</PageTitle>
<h1>Cars</h1>

@if (Cars != null)
{
    <div class="row">
        @foreach (var item in Cars)
        {
            <div class="col-md-4 mb-3">
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title">@item.Title</h5>
                        <p class="card-text">Year: @item.Year</p>
                        <p class="card-text">Price: @item.Price €</p>
                        <button class="btn btn-danger" @onclick="() => Delete(item.Id)">Delete</button>
                    </div>
                </div>
            </div>
        }
    </div>
}

@code {
    private List<Car>? Cars { get; set; }

    protected override void OnInitialized()
    {
        Cars = bus.GetCarsSortedByPrice();
    }

    private void Delete(int id)
    {
        bus.DeleteCar(id);
        Cars = bus.GetCarsSortedByPrice();
    }
} *@

@*<div class="container mt-4">
    <h3>Odaberi svoj omiljeni parfem</h3>

    <div class="mb-3">
        <label class="form-label">Lista parfema:</label>
        
        <select class="form-select" @bind="odabraniParfemId">
            
            <option value="0">-- Molimo odaberite parfem --</option>
            
            @if (PerfumesList != null)
            {
                @foreach (var p in PerfumesList)
                {
                    // value="@p.Id" je ono sto C# pamti, a @p.Name je ono sto korisnik cita!
                    <option value="@p.Id">@p.Name - @p.Brand (@p.Price din)</option>
                }
            }
        </select>
    </div>

    <button class="btn btn-primary" @onclick="PrikaziIzbor">Potvrdi izbor</button>

    <p class="mt-3 fw-bold text-success">@poruka</p>
</div>

@code {
    // Lista kojom punimo padajući meni
    private List<Perfume>? PerfumesList { get; set; }

    // OVO JE KLJUČ: Ovde čuvamo ID onog parfema koji je korisnik kliknuo u meniju!
    private int odabraniParfemId; 
    
    private string poruka = "";

    protected override void OnInitialized()
    {
        // Punimo listu iz baze cim se stranica ucita
        PerfumesList = bus.GetAllPerfumes();
    }

    private void PrikaziIzbor()
    {
        // Proveravamo da li je korisnik zaista nesto izabrao ili je ostavio na "-- Odaberite --"
        if (odabraniParfemId == 0)
        {
            poruka = "Morate izabrati parfem iz padajuce liste!";
        }
        else
        {
            // Ako jeste izabrao, ispisujemo ID. 
            // Na kolokvijumu bi ovde npr. poslao ovaj ID u bus.DeletePerfume(odabraniParfemId)
            poruka = $"Uspesno ste odabrali parfem sa ID brojem: {odabraniParfemId}";
        }
    }
}*@